# Dependencies
markdown:            redcarpet
highlighter:         pygments

# Permalinks
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
relative_permalinks: true

# the single quotes are NOT a must.
title:               '食色'
tagline:             '至末'
description:         '熙熙攘攘' 

# Without setting custom URL, if user pages, then use "/"; if project page, use "/project-name". Note the slash issue. If you include trailing slash, then should omit duplicated slash when accessing files.
baseurl:             /
paginate:            5

# About/contact
author:
  name:              '食色'
  url:               http://blog.fangxiang.tk
  email:             '顽石@警幻仙境'

# Custom vars
version:             1.0.0
url		     http://blog.fangxiang.tk
